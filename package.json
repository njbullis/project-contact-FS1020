{
  "name": "project-contact",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "nodemon -i \"*.json\" --inspect server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nancy Bullis <njbullis@bell.net>",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.1"
  }
}
